# Default search parameters (can be overridden via CLI flags)
DEFAULT_SITES=indeed,linkedin,zip_recruiter,glassdoor
DEFAULT_RESULTS_WANTED=20
DEFAULT_COUNTRY=USA

# Output directory for scraped results
OUTPUT_DIR=./data
